[project]
name = "vhl-workspace"
version = "0.1.0"
description = "VHL Workspace Management"
dependencies = []

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["workspace"]
package-dir = {"workspace" = "."}
