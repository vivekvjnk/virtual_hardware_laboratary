[project]
name = "mcp-server"
version = "0.1.0"
description = "MCP Server for Virtual Hardware Laboratory"
dependencies = [
    "fastapi==0.110.0",
    "uvicorn",
    "pydantic",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"