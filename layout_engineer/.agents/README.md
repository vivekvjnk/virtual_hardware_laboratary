# Layout Engineer Skills Directory

This directory stores Pi agent skill definition folders for the PCB Layout Engineer agent.
Each skill folder contains a `SKILL.md` document (and optional scripts/tool definitions) that Pi loads at runtime.
